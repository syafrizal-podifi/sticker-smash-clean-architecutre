/**
 * Route only; screen implementation lives in presentation/screens.
 * In Swift: route/screen registration; the actual VC is defined elsewhere.
 */
export { default } from '@/presentation/screens/HomeScreen';
