/**
 * Route only; screen implementation lives in presentation/home (feature-first).
 * In Swift: route/screen registration; the actual VC is defined elsewhere.
 */
export { default } from '@/presentation/home/HomeScreen';
